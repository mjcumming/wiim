-r requirements_dev.txt

# Home Assistant Core compatible test dependencies
# These versions are aligned with HA Core 2024.12+ requirements

# Testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
# pytest-timeout is included in pytest-homeassistant-custom-component
# Using the version required by HA Core for Python 3.13 compatibility
pytest-homeassistant-custom-component>=0.12.0

# Test utilities
aresponses>=2.1.6
freezegun>=1.2.2
zeroconf

# Note: async-upnp-client is pulled in by homeassistant dependency
# Removing strict pin to avoid version conflicts with HA Core

# Code quality
flake8>=6.0.0
ruff>=0.4.2
