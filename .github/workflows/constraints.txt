# Constraint file for GitHub Actions workflow
# CRITICAL: MINIMAL constraints to avoid pip resolver depth issues
# Only pin what's absolutely necessary to avoid conflicts

# pytest-homeassistant-custom-component and its core dependencies
pytest-homeassistant-custom-component==0.13.251
pytest==8.3.5
coverage==7.13.2

# Fix for Python 3.13 compatibility (pycares/aiodns issue)
pycares==4.4.0
aiodns==3.4.0
